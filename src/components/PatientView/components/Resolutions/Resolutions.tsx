import React, { FC } from "react";

import Toolbar from "../../../common/Toolbar/Toolbar";

const Resolutions: FC = () => (
  <>
    <Toolbar title="My Resolutions" />
  </>
);

export default Resolutions;
